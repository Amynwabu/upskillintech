services:
  mysql:
    image: mysql:8.4
    container_name: upskillintech-mysql-dev
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-upskillintech}
      MYSQL_USER: ${MYSQL_USER:-upskill}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?Set MYSQL_PASSWORD in the ignored .env file}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?Set MYSQL_ROOT_PASSWORD in the ignored .env file}
    ports:
      - "${MYSQL_PORT:-3307}:3306"
    volumes:
      - upskillintech_mysql_dev:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u root -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s

volumes:
  upskillintech_mysql_dev:
    name: upskillintech_mysql_dev
